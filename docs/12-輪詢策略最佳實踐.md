# 🔄 輪詢策略最佳實踐工作流

> **向量檢索標籤**：`polling`, `rotation`, `round-robin`, `fallback`, `rate-limit`, `輪詢`, `輪替`, `最佳實踐`
> **資料來源**：10 輪 × 5 維度深度搜尋 + 實戰驗證

---

## 📋 輪詢策略總覽

```
┌─────────────────────────────────────────────────────────────────┐
│                    輪詢策略架構                                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                      任務分發器                           │   │
│  │         同一問題 → 多個模型/帳號                          │   │
│  └────────────────────────┬─────────────────────────────────┘   │
│                           │                                     │
│         ┌─────────────────┼─────────────────┬───────────────┐   │
│         ▼                 ▼                 ▼               ▼   │
│  ┌──────────┐      ┌──────────┐      ┌──────────┐    ┌─────────┐│
│  │ Claude   │      │ GPT-5    │      │ Gemini   │    │ 本地    ││
│  │ 多帳號   │      │ 多帳號   │      │ 多帳號   │    │ Ollama  ││
│  └────┬─────┘      └────┬─────┘      └────┬─────┘    └────┬────┘│
│       │                 │                 │               │     │
│       └─────────────────┴─────────────────┴───────────────┘     │
│                           │                                     │
│                           ▼                                     │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │              策略選擇器                                    │   │
│  │  Round-Robin │ Rate-Aware │ Cost-Aware │ Quality-Based   │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 1️⃣ Round-Robin 輪替策略

### 適用場景
- 多個等效帳號/API Key
- 負載均衡
- 避免單一帳號 Rate Limit

### 配置範例

```json5
// ~/.openclaw/openclaw.json
{
  "smart-polling": {
    "accounts": {
      "google": {
        // 4 個 Google 帳號輪替
        "accounts": [
          "yyfashionfb@yyfashionlalala.com",  // 企業版
          "sangrong01@gmail.com",              // 個人高級
          "s597312288@gmail.com",              // 備用 1
          "s5973122888@gmail.com"              // 備用 2
        ],
        "rotation": "round-robin"
      }
    }
  }
}
```

### Python 實現

```python
"""
Round-Robin Key Rotation Service
Thread-safe implementation using threading.Lock.
"""
import threading
from typing import List, Optional
from dataclasses import dataclass, field

@dataclass
class AccountKeyPool:
    """帳號金鑰池"""
    account_email: str
    keys: List[str]
    current_index: int = 0
    
    def get_next_key(self) -> str:
        """取得下一個金鑰（Round-Robin）"""
        key = self.keys[self.current_index]
        self.current_index = (self.current_index + 1) % len(self.keys)
        return key

class KeyRotator:
    """金鑰輪替器"""
    _instance = None
    _lock = threading.Lock()
    
    def __init__(self):
        self.pools: dict[str, AccountKeyPool] = {}
        self.pool_index = 0
        
    @classmethod
    def get_instance(cls) -> 'KeyRotator':
        if cls._instance is None:
            with cls._lock:
                if cls._instance is None:
                    cls._instance = cls()
        return cls._instance
    
    def add_pool(self, account: str, keys: List[str]):
        """新增帳號金鑰池"""
        self.pools[account] = AccountKeyPool(account, keys)
    
    def get_next(self) -> tuple[str, str]:
        """取得下一個帳號和金鑰"""
        with self._lock:
            pool_names = list(self.pools.keys())
            pool_name = pool_names[self.pool_index]
            pool = self.pools[pool_name]
            
            self.pool_index = (self.pool_index + 1) % len(pool_names)
            
            return pool.account_email, pool.get_next_key()

# 使用方式
rotator = KeyRotator.get_instance()
rotator.add_pool("s597312288@gmail.com", ["key1", "key2", "key3"])
rotator.add_pool("s5973122888@gmail.com", ["key4", "key5", "key6"])

account, key = rotator.get_next()
```

---

## 2️⃣ Rate-Limit Aware 策略

### 適用場景
- API 有配額限制
- 需要自動切換避免 429 錯誤
- 多 Provider 備援

### 配置範例

```json5
{
  "agents": {
    "defaults": {
      "model": {
        "primary": "anthropic/claude-opus-4-5",
        "fallbacks": [
          "anthropic/claude-sonnet-4-5",    // 同 Provider 備援
          "openai/gpt-5.2",                  // 不同 Provider
          "openrouter/openrouter/auto",      // 自動選擇
          "google/gemini-2.5-pro"            // 最後備援
        ]
      },
      
      // Rate Limit 配置
      "rateLimit": {
        "retryAfter": true,              // 遵循 Retry-After header
        "backoff": {
          "initial": 1000,               // 初始等待 1 秒
          "multiplier": 2,               // 指數退避
          "maxDelay": 60000              // 最大等待 60 秒
        },
        "cooldown": {
          "onError": 300000,             // 錯誤後冷卻 5 分鐘
          "onQuotaExceeded": 3600000     // 配額用完冷卻 1 小時
        }
      }
    }
  }
}
```

### 錯誤處理流程

```
請求 → Primary Model
        │
        ├─ 成功 → 返回結果
        │
        └─ 失敗 (429/5xx)
            │
            ├─ 有 Retry-After → 等待後重試
            │
            └─ 無 Retry-After
                │
                ├─ 重試次數 < 3 → 指數退避重試
                │
                └─ 重試次數 >= 3
                    │
                    └─ 切換到 Fallback Model
                        │
                        ├─ Fallback 成功 → 返回結果
                        │
                        └─ 所有 Fallback 失敗
                            │
                            └─ 返回錯誤
```

### Python 實現

```python
import asyncio
import time
from typing import List, Dict, Optional

class RateLimitAwarePoller:
    """Rate Limit 感知輪詢器"""
    
    def __init__(self):
        self.cooldowns: Dict[str, float] = {}  # model -> cooldown_until
        self.backoff_state: Dict[str, int] = {}  # model -> retry_count
        
    def is_available(self, model: str) -> bool:
        """檢查模型是否可用"""
        if model in self.cooldowns:
            if time.time() < self.cooldowns[model]:
                return False
            del self.cooldowns[model]
        return True
    
    def mark_rate_limited(self, model: str, retry_after: Optional[int] = None):
        """標記模型被 Rate Limit"""
        cooldown = retry_after or 300  # 預設 5 分鐘
        self.cooldowns[model] = time.time() + cooldown
        
    async def query_with_fallback(
        self, 
        question: str, 
        models: List[str]
    ) -> Dict:
        """帶 Fallback 的查詢"""
        
        for model in models:
            if not self.is_available(model):
                continue
                
            try:
                result = await self._query_model(question, model)
                self.backoff_state[model] = 0  # 重置重試計數
                return {"model": model, "result": result}
                
            except RateLimitError as e:
                self.mark_rate_limited(model, e.retry_after)
                continue
                
            except Exception as e:
                # 指數退避
                retry_count = self.backoff_state.get(model, 0)
                if retry_count < 3:
                    wait_time = (2 ** retry_count)
                    await asyncio.sleep(wait_time)
                    self.backoff_state[model] = retry_count + 1
                continue
        
        raise Exception("所有模型都不可用")
```

---

## 3️⃣ Cost-Aware 成本感知策略

### 適用場景
- 有預算限制
- 需要平衡成本和品質
- 簡單任務用便宜模型

### 模型成本表

| 模型 | 輸入 $/1M tokens | 輸出 $/1M tokens | 推薦用途 |
|------|-----------------|-----------------|----------|
| gemini-flash | $0.01 | $0.02 | Heartbeat、狀態檢查 |
| claude-haiku | $0.25 | $1.25 | 簡單對話 |
| gpt-4o-mini | $0.15 | $0.60 | 快速回應 |
| claude-sonnet | $3.00 | $15.00 | 平衡任務 |
| gpt-5 | $5.00 | $20.00 | 複雜推理 |
| claude-opus | $15.00 | $75.00 | 頂級品質 |

### 配置範例

```json5
{
  "smart-polling": {
    "costAware": {
      "enabled": true,
      "budget": {
        "daily": 10.00,      // 每日預算 $10
        "perTask": 0.50      // 單任務上限 $0.50
      },
      
      // 成本階梯
      "tiers": {
        "cheap": ["gemini-flash", "claude-haiku", "gpt-4o-mini"],
        "balanced": ["claude-sonnet", "gpt-5"],
        "premium": ["claude-opus"]
      },
      
      // 任務分類規則
      "routing": {
        "heartbeat": "cheap",
        "simple_qa": "cheap",
        "code_review": "balanced",
        "architecture": "premium"
      }
    }
  }
}
```

### Python 實現

```python
class CostAwarePoller:
    """成本感知輪詢器"""
    
    MODEL_COSTS = {
        "gemini-flash": 0.01,
        "claude-haiku": 0.02,
        "gpt-4o-mini": 0.02,
        "claude-sonnet": 0.10,
        "gpt-5": 0.20,
        "claude-opus": 0.50
    }
    
    def __init__(self, daily_budget: float = 10.0):
        self.daily_budget = daily_budget
        self.spent_today = 0.0
        
    async def poll(self, question: str, max_cost: float = 1.0) -> Dict:
        """在預算內輪詢"""
        
        # 按成本排序
        sorted_models = sorted(
            self.MODEL_COSTS.items(), 
            key=lambda x: x[1]
        )
        
        responses = []
        
        for model, cost in sorted_models:
            # 檢查預算
            if self.spent_today + cost > self.daily_budget:
                break
            if cost > max_cost:
                continue
                
            response = await self._query_model(question, model)
            responses.append(response)
            self.spent_today += cost
            
            # 便宜模型達成共識就停止
            if len(responses) >= 2:
                if self._check_consensus(responses):
                    break
        
        return {
            "responses": responses,
            "total_cost": self.spent_today,
            "models_used": [r['model'] for r in responses]
        }
```

---

## 4️⃣ Quality-Based 品質策略

### 適用場景
- 重要決策需要驗證
- 多模型交叉檢驗
- 需要最佳答案

### 策略類型

| 策略 | 說明 | 適用場景 |
|------|------|----------|
| **Vote** | 多數投票 | 事實性問題 |
| **Quality** | 品質評分 | 開放式問題 |
| **Synthesis** | 合成整合 | 複雜問題 |
| **Layered** | 分層驗證 | 高風險決策 |

### 配置範例

```json5
{
  "smart-polling": {
    "defaults": {
      "models": [
        "anthropic/claude-opus-4-5",
        "openai/gpt-5.2",
        "google/gemini-2.5-pro"
      ],
      "strategy": "quality",
      "timeout": 30,
      "minResponses": 2
    },
    
    // 預設模型組合
    "presets": {
      "fast": ["claude-haiku", "gpt-4o-mini", "gemini-flash"],
      "balanced": ["claude-sonnet", "gpt-5", "gemini-pro"],
      "premium": ["claude-opus", "gpt-5", "gemini-ultra"]
    }
  }
}
```

### 使用方式

```bash
# 預設輪詢
/super smart-polling "這段程式碼有什麼潛在問題？"

# 指定模型
/super smart-polling "問題" --models="claude-opus,gpt-5,gemini-pro"

# 指定策略
/super smart-polling "問題" --strategy=vote

# 使用預設組合
/super smart-polling "問題" --preset=premium
```

---

## 5️⃣ 高效工作流配置

### 完整配置範例

```json5
// ~/.openclaw/openclaw.json
{
  "agents": {
    "defaults": {
      "model": {
        // 主要模型
        "primary": "copilot-proxy/claude-opus-4.5",
        
        // 備援鏈（按順序嘗試）
        "fallbacks": [
          "copilot-proxy/claude-sonnet-4.5",
          "copilot-proxy/gpt-5.2",
          "copilot-proxy/gemini-2.5-pro-vertex-enterprise",
          "openrouter/openrouter/auto"  // 最後用 OpenRouter 自動選
        ]
      },
      
      // 模型別名（方便切換）
      "models": {
        "copilot-proxy/claude-opus-4.5": { "alias": "opus" },
        "copilot-proxy/claude-sonnet-4.5": { "alias": "sonnet" },
        "copilot-proxy/gpt-5.2": { "alias": "gpt5" },
        "copilot-proxy/gemini-2.5-pro-vertex-enterprise": { "alias": "gemini-企業" }
      }
    }
  },
  
  "smart-polling": {
    // 多帳號輪替
    "accounts": {
      "anthropic": {
        "primary": "${ANTHROPIC_API_KEY}",
        "fallback": "${ANTHROPIC_API_KEY_2}"
      },
      "google": {
        "accounts": [
          "yyfashionfb@yyfashionlalala.com",
          "sangrong01@gmail.com",
          "s597312288@gmail.com",
          "s5973122888@gmail.com"
        ],
        "rotation": "round-robin"
      }
    },
    
    // 成本感知
    "costAware": {
      "enabled": true,
      "budget": { "daily": 20.00 }
    }
  }
}
```

### 切換模型指令

```bash
# 切換到特定模型
/model opus
/model gpt5
/model gemini-企業

# 查看當前模型
/model

# 查看可用模型
/models
```

---

## 🎯 最佳實踐總結

### ✅ DO（應該做）

1. **多 Provider 備援** - 不要只依賴單一 Provider
2. **Rate Limit 處理** - 實現指數退避和自動切換
3. **成本監控** - 設定預算上限
4. **日誌記錄** - 記錄每次輪詢的模型和結果
5. **健康檢查** - 定期檢查各模型可用性

### ❌ DON'T（不要做）

1. **單一帳號** - 容易被 Rate Limit
2. **無限重試** - 會浪費配額
3. **忽略成本** - 可能產生意外費用
4. **硬編碼金鑰** - 使用環境變數

---

## 📚 相關文檔

- [04-多模型整合.md](./04-多模型整合.md)
- [07-MCP工具完整手冊.md](./07-MCP工具完整手冊.md)
- [11-完整規範框架.md](./11-完整規範框架.md)

---

*最後更新：2026-02-04*
