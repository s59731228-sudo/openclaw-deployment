# 🔧 MCP 工具完整手冊

> **向量檢索標籤**：`mcp`, `tools`, `api`, `parameters`, `工具`, `參數`

---

## 📋 工具總覽

| 工具名稱 | 中文說明 | 核心用途 |
|----------|----------|----------|
| `openclaw_install` | 安裝 OpenClaw | 一鍵安裝（npm/Docker） |
| `openclaw_health` | 健康檢查 | 診斷系統狀態 |
| `openclaw_config` | 配置管理 | 查看/修改配置 |
| `openclaw_deploy` | 一鍵部署 | 啟動網關服務 |
| `openclaw_docs` | 查詢文檔 | 檢索教學資料 |

---

## 🔧 工具一：openclaw_install

### 功能說明
自動安裝 OpenClaw CLI 並執行初始化設置。

### 參數定義

| 參數 | 類型 | 必填 | 預設值 | 說明 |
|------|------|------|--------|------|
| `method` | string | 否 | `"npm"` | 安裝方式：`"npm"` 或 `"docker"` |

### 調用方式

```python
# 方式一：npm 安裝（預設）
openclaw_install()
openclaw_install("npm")

# 方式二：Docker 安裝
openclaw_install("docker")
```

### 返回格式

```json
{
  "success": true,
  "message": "OpenClaw 安裝完成",
  "install_output": "...",
  "onboard_output": "..."
}
```

### 流程圖

```
openclaw_install("npm")
    │
    ├─ 1. npm install -g openclaw@latest
    │
    ├─ 2. openclaw onboard --install-daemon
    │
    └─ 3. 返回安裝結果

openclaw_install("docker")
    │
    ├─ 1. 檢查 docker-compose.yml 存在
    │
    ├─ 2. docker compose up -d
    │
    └─ 3. 返回容器狀態
```

---

## 🔧 工具二：openclaw_health

### 功能說明
執行全面健康檢查，診斷系統各組件狀態。

### 參數定義

| 參數 | 類型 | 必填 | 預設值 | 說明 |
|------|------|------|--------|------|
| （無參數） | - | - | - | 自動檢查所有項目 |

### 調用方式

```python
openclaw_health()
```

### 返回格式

```json
{
  "success": true,
  "checks": {
    "node": {"status": "✅", "version": "v20.x.x"},
    "openclaw": {"status": "✅", "version": "1.x.x"},
    "api_keys": {
      "ANTHROPIC_API_KEY": "✅ 已配置",
      "OPENAI_API_KEY": "✅ 已配置",
      "GOOGLE_API_KEY": "❌ 未配置"
    },
    "doctor": {"status": "✅", "output": "..."}
  },
  "summary": "健康檢查完成"
}
```

### 檢查項目

| 項目 | 檢查內容 | 通過條件 |
|------|----------|----------|
| Node.js | 版本號 | >= 18.0 |
| OpenClaw | 安裝狀態 | 已安裝 |
| API Keys | 環境變數 | 已設置 |
| Doctor | 系統診斷 | 無錯誤 |

---

## 🔧 工具三：openclaw_config

### 功能說明
管理 OpenClaw 配置檔，支援查看、取得、設定操作。

### 參數定義

| 參數 | 類型 | 必填 | 預設值 | 說明 |
|------|------|------|--------|------|
| `action` | string | 否 | `"show"` | 操作類型：`"show"` / `"get"` / `"set"` |
| `key` | string | 條件 | `None` | 配置鍵（get/set 時必填） |
| `value` | string | 條件 | `None` | 配置值（set 時必填） |

### 調用方式

```python
# 顯示所有配置
openclaw_config("show")

# 取得特定配置
openclaw_config("get", "models.default")
openclaw_config("get", "gateway.port")

# 設定配置
openclaw_config("set", "gateway.port", "18790")
openclaw_config("set", "models.default", "gpt-4o")
```

### 返回格式

```json
// show
{
  "success": true,
  "config": { "models": {...}, "gateway": {...} }
}

// get
{
  "success": true,
  "key": "models.default",
  "value": "claude-sonnet-4-20250514"
}

// set
{
  "success": true,
  "message": "已設定 gateway.port = 18790"
}
```

### 常用配置路徑

| 路徑 | 說明 | 範例值 |
|------|------|--------|
| `gateway.port` | 網關端口 | `18789` |
| `models.default` | 預設模型 | `claude-sonnet-4-20250514` |
| `models.routing.strategy` | 路由策略 | `fallback` |
| `memory.enabled` | 記憶功能 | `true` |
| `security.rateLimit.enabled` | 速率限制 | `true` |

---

## 🔧 工具四：openclaw_deploy

### 功能說明
一鍵部署 OpenClaw 網關服務。

### 參數定義

| 參數 | 類型 | 必填 | 預設值 | 說明 |
|------|------|------|--------|------|
| `method` | string | 否 | `"docker"` | 部署方式：`"docker"` / `"npm"` / `"daemon"` |
| `port` | int | 否 | `18789` | 網關端口 |

### 調用方式

```python
# Docker 部署（預設）
openclaw_deploy()
openclaw_deploy("docker", 18789)

# npm 前台執行
openclaw_deploy("npm", 18790)

# 背景守護程序
openclaw_deploy("daemon", 18789)
```

### 返回格式

```json
{
  "success": true,
  "message": "Docker 部署完成，端口 18789",
  "output": "..."
}
```

### 部署方式比較

| 方式 | 隔離性 | 持久性 | 適用場景 |
|------|--------|--------|----------|
| `docker` | ⭐⭐⭐ | ⭐⭐⭐ | 生產環境 |
| `npm` | ⭐ | ⭐ | 快速測試 |
| `daemon` | ⭐⭐ | ⭐⭐⭐ | 開發環境 |

---

## 🔧 工具五：openclaw_docs

### 功能說明
查詢教學文檔，支援主題檢索。

### 參數定義

| 參數 | 類型 | 必填 | 預設值 | 說明 |
|------|------|------|--------|------|
| `topic` | string | 否 | `None` | 主題名稱（中文或英文） |

### 調用方式

```python
# 列出所有文檔
openclaw_docs()

# 查詢特定主題（中文）
openclaw_docs("快速入門")
openclaw_docs("部署方式")
openclaw_docs("安全配置")
openclaw_docs("多模型")
openclaw_docs("技能系統")
openclaw_docs("記憶系統")

# 查詢特定主題（英文）
openclaw_docs("quick-start")
openclaw_docs("deployment")
openclaw_docs("security")
```

### 返回格式

```json
// 列出文檔
{
  "success": true,
  "available_docs": ["01-快速入門.md", "02-部署方式.md", ...],
  "usage": "使用 openclaw_docs('主題名稱') 查詢特定文檔"
}

// 查詢文檔
{
  "success": true,
  "topic": "部署方式",
  "content": "# 🐳 OpenClaw 部署方式比較\n..."
}
```

### 主題對照表

| 中文主題 | 英文主題 | 對應文檔 |
|----------|----------|----------|
| 快速入門 | quick-start | 01-快速入門.md |
| 部署方式 | deployment | 02-部署方式.md |
| 安全配置 | security | 03-安全配置.md |
| 多模型 | multi-model | 04-多模型整合.md |
| 技能系統 | skills | 05-技能系統.md |
| 記憶系統 | memory | 06-記憶系統.md |

---

## 🎮 框架架構圖

```
┌─────────────────────────────────────────────────────────┐
│                  OpenClaw 智慧老師 MCP                    │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐     │
│  │   Install   │  │   Health    │  │   Config    │     │
│  │   安裝工具   │  │   健康檢查   │  │   配置管理   │     │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘     │
│         │                │                │             │
│  ┌──────┴──────┐  ┌──────┴──────┐  ┌──────┴──────┐     │
│  │   Deploy    │  │    Docs     │  │   (擴展)    │     │
│  │   一鍵部署   │  │   文檔查詢   │  │   未來工具   │     │
│  └─────────────┘  └─────────────┘  └─────────────┘     │
│                                                         │
├─────────────────────────────────────────────────────────┤
│                     底層服務                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐     │
│  │ run_command │  │  File I/O   │  │   JSON      │     │
│  │  指令執行    │  │  檔案讀寫    │  │   解析      │     │
│  └─────────────┘  └─────────────┘  └─────────────┘     │
└─────────────────────────────────────────────────────────┘
```

---

## 📊 調用流程圖

```
用戶請求
    │
    ▼
┌─────────────────┐
│  AI 理解意圖     │
└────────┬────────┘
         │
         ▼
┌─────────────────┐     ┌─────────────────┐
│  載入技能       │────▶│  skill.md       │
│  /skill openclaw│     │  知識庫載入      │
└────────┬────────┘     └─────────────────┘
         │
         ▼
┌─────────────────┐
│  選擇 MCP 工具   │
└────────┬────────┘
         │
    ┌────┴────┬────────┬────────┬────────┐
    ▼         ▼        ▼        ▼        ▼
 install   health   config   deploy    docs
    │         │        │        │        │
    ▼         ▼        ▼        ▼        ▼
┌─────────────────────────────────────────┐
│           執行並返回結果                  │
└─────────────────────────────────────────┘
         │
         ▼
┌─────────────────┐
│  AI 整理回覆     │
└────────┬────────┘
         │
         ▼
    用戶收到結果
```

---

*文檔版本：1.0*
