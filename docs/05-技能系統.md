# ⚡ OpenClaw 技能系統

> **向量檢索標籤**：`skills`, `mcp`, `plugins`, `skill.md`, `技能`, `自定義`

---

## 📋 技能系統概述

技能（Skill）是 OpenClaw 的擴展機制，讓 AI 具備特定領域的專業知識和工具。

| 組件 | 說明 |
|------|------|
| **skill.md** | 技能定義檔，包含指令和知識 |
| **MCP 工具** | 可執行的函數，透過 MCP 協議呼叫 |
| **知識庫** | 向量檢索友好的文檔 |

---

## 📁 技能目錄結構

```
~/.openclaw/skills/
├── my-skill/
│   ├── skill.md          # 技能定義（必要）
│   ├── mcp/              # MCP 工具（選用）
│   │   └── tools.py
│   ├── docs/             # 知識文檔（選用）
│   │   └── guide.md
│   └── examples/         # 使用範例（選用）
```

---

## 📝 skill.md 格式

### 基本結構

```markdown
---
name: my-skill
description: "技能簡短描述（會顯示在技能列表）"
metadata:
  {
    "version": "1.0",
    "author": "your-name",
    "tags": ["tag1", "tag2"]
  }
---

# 技能標題

> **中文備註**：技能用途說明
> **使用方式**：`/skill my-skill`

---

## 功能說明

這裡寫技能的詳細功能...

## 使用範例

```bash
# 範例指令
command --flag
```

## 常見問題

### Q: 問題一？
答案...
```

---

### 完整範例

```markdown
---
name: ecount-order-builder
description: "📦 ECOUNT 訂單建立 - 從合約建立訂貨單，避免欄位錯誤"
metadata:
  {
    "version": "1.0",
    "tags": ["ecount", "order", "訂單"]
  }
---

# 📦 ECOUNT 訂單建立技能

> **中文備註**：自動從合約資料建立 ECOUNT 訂貨單
> **使用方式**：`/skill ecount-order-builder`

---

## 🎯 功能

1. 解析合約內容
2. 對應 ECOUNT 欄位
3. 自動計算金額（含稅）
4. 生成可匯入的 JSON

---

## 📋 必填欄位

| 欄位 | 說明 | 範例 |
|------|------|------|
| PROD_CD | 品項代碼 | "10001" |
| QTY | 數量 | 100 |
| PRICE | 單價 | 150 |

---

## ⚠️ 注意事項

- 營業稅固定 5%
- 運費需作為獨立品項
```

---

## 🔧 MCP 工具開發

### 工具函數格式

```python
def tool_name(param1: str, param2: int = 10) -> dict:
    """
    工具說明
    
    參數:
        param1: 參數說明
        param2: 參數說明（預設 10）
    
    使用範例:
        tool_name("value", 20)
    """
    # 實作邏輯
    return {
        "success": True,
        "result": "..."
    }
```

### 註冊工具

```python
TOOLS = {
    "tool_name": tool_name,
    "another_tool": another_tool,
}
```

---

## 📥 載入技能

### 方式一：指令載入

```bash
/skill my-skill
```

### 方式二：對話中提及

AI 會根據對話內容自動載入相關技能。

### 方式三：設定自動載入

在 `~/.openclaw/openclaw.json`：

```json
{
  "skills": {
    "autoLoad": ["my-skill", "another-skill"],
    "directory": "~/.openclaw/skills"
  }
}
```

---

## 🌐 MCP (Model Context Protocol) 整合

### 什麼是 MCP？

MCP 是標準化的 AI 工具協議，讓 AI 可以：
- 呼叫外部函數
- 存取檔案系統
- 連接資料庫
- 執行系統指令

### 配置 MCP Server

在 `~/.claude/settings.json` 或 `~/.openclaw/openclaw.json`：

```json
{
  "mcpServers": {
    "my-mcp": {
      "command": "python3",
      "args": ["~/.openclaw/skills/my-skill/mcp/server.py"],
      "env": {
        "MY_API_KEY": "${MY_API_KEY}"
      }
    }
  }
}
```

---

## 📚 社群技能推薦

| 技能 | 說明 | 安裝 |
|------|------|------|
| `git-master` | Git 操作專家 | 內建 |
| `playwright` | 瀏覽器自動化 | 內建 |
| `ecount-api` | ECOUNT ERP 整合 | 自定義 |
| `openclaw-deployment` | OpenClaw 部署教學 | 自定義 |

---

## 🛠️ 開發技能的最佳實踐

### 1. 命名規範
- 使用小寫和連字號：`my-skill-name`
- 描述要簡潔但明確

### 2. 文檔格式
- 使用中文備註說明用途
- 提供使用方式範例
- 加入向量檢索標籤

### 3. MCP 工具
- 每個函數都要有 docstring
- 返回統一格式 `{"success": bool, ...}`
- 處理錯誤情況

### 4. 測試
- 在載入前測試 skill.md 語法
- 測試 MCP 工具函數
- 驗證向量檢索標籤

---

## ❓ 常見問題

### Q: 技能載入失敗？

檢查 skill.md 格式：
```bash
# 驗證 YAML frontmatter
cat ~/.openclaw/skills/my-skill/skill.md | head -20
```

### Q: MCP 工具無法呼叫？

1. 檢查 settings.json 配置
2. 確認 Python 路徑正確
3. 查看錯誤日誌

### Q: 如何分享技能？

將技能目錄打包或推送到 GitHub：
```bash
cd ~/.openclaw/skills/my-skill
git init && git add . && git commit -m "Initial commit"
```

---

*文檔版本：1.0*
