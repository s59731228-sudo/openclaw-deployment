# 🚀 OpenClaw 快速入門

> **預計時間**：5 分鐘
> **難度等級**：⭐ 入門
> **向量檢索標籤**：`openclaw`, `quick-start`, `installation`, `快速入門`

---

## 📋 前置需求

| 需求 | 最低版本 | 檢查指令 |
|------|----------|----------|
| Node.js | 18.0+ | `node -v` |
| npm | 9.0+ | `npm -v` |
| 作業系統 | macOS / Linux / Windows | - |

---

## 🔧 安裝步驟

### 步驟 1：安裝 OpenClaw CLI

```bash
# 全域安裝
npm install -g openclaw@latest

# 驗證安裝
openclaw --version
```

**預期輸出**：`openclaw v1.x.x`

---

### 步驟 2：執行引導設置

```bash
# 執行 onboard 引導程序
openclaw onboard

# 如需安裝背景守護程序（推薦）
openclaw onboard --install-daemon
```

**這步會做什麼**：
- 創建 `~/.openclaw/` 配置目錄
- 生成預設 `openclaw.json` 配置檔
- （可選）安裝系統守護程序

---

### 步驟 3：配置 API 金鑰

```bash
# 方式一：環境變數（推薦）
export ANTHROPIC_API_KEY="sk-ant-..."
export OPENAI_API_KEY="sk-..."
export GOOGLE_API_KEY="AIza..."

# 方式二：寫入 ~/.zshrc 或 ~/.bashrc
echo 'export ANTHROPIC_API_KEY="sk-ant-..."' >> ~/.zshrc
source ~/.zshrc
```

---

### 步驟 4：啟動網關

```bash
# 啟動 OpenClaw 網關
openclaw gateway --port 18789

# 背景執行（使用 daemon）
openclaw gateway --daemon
```

**預期輸出**：
```
🦞 OpenClaw Gateway starting...
✓ Listening on http://localhost:18789
✓ Ready to accept connections
```

---

### 步驟 5：健康檢查

```bash
# 檢查系統狀態
openclaw doctor

# 檢查連線狀態
openclaw health

# 查看配置
openclaw config show
```

---

## ✅ 驗證安裝成功

執行以下指令，全部 ✓ 即安裝成功：

```bash
openclaw doctor
```

**預期輸出**：
```
✓ Node.js version: 20.x.x
✓ npm version: 10.x.x
✓ OpenClaw installed: 1.x.x
✓ Config directory: ~/.openclaw/
✓ API keys configured: anthropic, openai
✓ Gateway status: running
```

---

## 🔗 下一步

- [02-部署方式.md](02-部署方式.md) - 了解 Docker / npm / Nix 部署差異
- [03-安全配置.md](03-安全配置.md) - 生產環境安全設定

---

## ❓ 常見問題

### Q: 安裝時出現權限錯誤？

```bash
# 使用 npx 暫時執行
npx openclaw@latest onboard

# 或修復 npm 權限
sudo chown -R $(whoami) ~/.npm
```

### Q: API 金鑰無效？

```bash
# 檢查金鑰格式
echo $ANTHROPIC_API_KEY | head -c 10

# Anthropic 金鑰應以 sk-ant- 開頭
# OpenAI 金鑰應以 sk- 開頭
```

### Q: 網關無法啟動？

```bash
# 檢查端口佔用
lsof -i :18789

# 換用其他端口
openclaw gateway --port 18790
```

---

*文檔版本：1.0*
*適用 OpenClaw：1.x*
